<script>
    $(function () {
        $('[data-toggle="popover"]').popover()
    })
</script>

<br /><br /><br />
<div class="h-100 row align-items-center text-light">
    <div class="col parent">
        <br />
        <div class="container">
            <div class="row">
                <div class="col">
                    <img src='{{user.userImage}}'
                        style="border-radius: 50%; height: 50px; width: 50px; display: inline;" />
                    <h4>{{ user.givenName }}'s Profile</h4>

                    <p>{{user.email}}</p>
                </div>
                <div class="col">
                    <form action="#">
                        <button type="submit" class="btn btn-warning">Account Settings</button>
                    </form><br/>
                    <form action="/users/logout">
                        <button type="submit" class="btn btn-danger">Log out</button>
                    </form>
                </div>
            </div>
        </div>

        <br />
        {{!-- <div class="container">
            {{#grouped_each 3 minedMovies}}
                {{#each minedMovies}}
                    <div class="col text-center mx-auto">
                        <a href="/browse/view?key={{ this.imdb_title_id }}" class="hvr-grow">
                            <img src="{{this.poster}}" alt="Poster Image" style="width: 100%; height: auto;" />
                        </a><br /><br />
                        <p>{{ this.title }}</p>
                    </div>
                {{/each}}
            {{/grouped_each}}
        </div> --}}

        <div class="row">
            {{#each minedMovies}}
            <div class="col text-center mx-auto">
                <a href="/browse/view?key={{ this.imdb_title_id }}" class="hvr-grow">
                    <img src="{{this.poster}}" alt="Poster Image" style="width: 100%; height: auto;" />
                </a><br /><br />
                <p>{{ this.title }}</p>
                {{!-- form messed with formatting, waiting.. --}}
                <button class="btn btn-success" type="submit" name="WatchLaterButton" value={{this.imdb_title_id}}>✔
                    Watched</button>
            </div>
            <br />
            {{/each}}
        </div>
    </div>
</div>